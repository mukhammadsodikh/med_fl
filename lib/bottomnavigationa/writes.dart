import 'package:flutter/material.dart';

class WritesScreen extends StatelessWidget {
  const WritesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This page is empty"),
      ),
    );
  }
}
